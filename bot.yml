name: jessbot
description: Make buddies better buddies
config:
  - name: FIREBASE_DB_URL
    friendly_name: Firebase database url
    info: A place to persist information
    default: NOT A VALID TOKEN
    global: true

  - name: FIREBASE_PROJECT_ID
    friendly_name: Firebase service account profile id
    default: NOT A VALID TOKEN
    global: true

  - name: FIREBASE_CLIENT_EMAIL
    friendly_name: Firebase service account client email
    default: NOT A VALID TOKEN
    global: true

  - name: FIREBASE_PRIVATE_KEY
    friendly_name: Firebase service account private key
    default: NOT A VALID TOKEN
    global: true
